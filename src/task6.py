def newton_symbol():
    """
    Napisz rekurencyjną funkcję liczącą symbol Newtona:

    Newton(n, k) = n! / (k! (n-k)!)

    Zastosuj definicję rekurencyjną:
    - Newton(n, 0) = 1
    - Newton(n, n) = 1
    - Newton(n, k) = Newton(n-1, k-1) + Newton(n-1, k)
    """
    pass